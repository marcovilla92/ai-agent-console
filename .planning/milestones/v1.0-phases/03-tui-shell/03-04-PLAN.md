---
phase: 03-tui-shell
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tui/app.py
  - src/tui/actions.py
  - src/tui/status_bar.py
  - tests/test_tui_keys.py
autonomous: true
gap_closure: true
requirements:
  - TUI-01
  - TUI-02
  - TUI-03
  - TUI-04
  - TUI-06
  - INFR-02

must_haves:
  truths:
    - "Pressing Ctrl+S with a prompt triggers start_agent_worker for the plan agent"
    - "Pressing Ctrl+P/E/R with a prompt triggers start_agent_worker for the corresponding agent"
    - "Status bar default hint text says Ctrl+S, not Ctrl+Enter"
  artifacts:
    - path: "src/tui/app.py"
      provides: "Wiring from run_agent() to start_agent_worker()"
      contains: "start_agent_worker"
    - path: "src/tui/actions.py"
      provides: "send_prompt() calls start_agent_worker for plan agent"
      contains: "start_agent_worker"
    - path: "src/tui/status_bar.py"
      provides: "Correct Ctrl+S hint text"
      contains: "Ctrl+S"
    - path: "tests/test_tui_keys.py"
      provides: "Tests verifying streaming worker is invoked from keyboard actions"
      contains: "start_agent_worker"
  key_links:
    - from: "src/tui/app.py:run_agent"
      to: "src/tui/streaming.start_agent_worker"
      via: "direct call after prepare_agent_run returns prompt"
      pattern: "start_agent_worker\\(self,\\s*agent_name,\\s*prompt\\)"
    - from: "src/tui/actions.py:send_prompt"
      to: "src/tui/streaming.start_agent_worker"
      via: "direct call after prepare_agent_run returns prompt"
      pattern: "start_agent_worker\\(app,\\s*.plan.,\\s*prompt\\)"
---

<objective>
Wire the streaming worker to keyboard actions, closing the one blocking gap from Phase 3 verification.

Purpose: The streaming worker (start_agent_worker in streaming.py) is fully implemented and tested but never called from any keyboard action path. All Ctrl+S/P/E/R shortcuts terminate at prepare_agent_run() without launching a Claude CLI subprocess. This plan connects the existing, working streaming machinery to the existing, working keyboard handlers.

Output: Keyboard shortcuts trigger real agent execution via start_agent_worker. Status bar shows correct Ctrl+S hint.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From src/tui/streaming.py — the function that needs to be called -->
```python
def start_agent_worker(
    app: AgentConsoleApp,
    agent_name: str,
    prompt: str,
    db: aiosqlite.Connection | None = None,
    session_id: int | None = None,
) -> Worker:
```

<!-- From src/tui/actions.py — current signatures -->
```python
AGENT_PANEL_MAP = {"plan": "plan-panel", "execute": "execute-panel", "review": "review-panel"}
def prepare_agent_run(app: AgentConsoleApp, agent_name: str) -> str | None:
def send_prompt(app: AgentConsoleApp) -> str | None:
def complete_agent_run(app: AgentConsoleApp, agent_name: str, success: bool = True) -> None:
```

<!-- From src/tui/app.py — methods that need modification -->
```python
def run_agent(self, agent_name: str) -> None:
    prompt = prepare_agent_run(self, agent_name)
    if prompt is None:
        return
    # Actual agent execution is handled by streaming worker (Plan 03-03)

def action_send_prompt(self) -> None:
    send_prompt(self)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire start_agent_worker into app.py and actions.py</name>
  <files>src/tui/app.py, src/tui/actions.py, src/tui/status_bar.py</files>
  <action>
Three targeted edits to close the streaming wiring gap:

1. **src/tui/app.py** — In the imports section, add `from src.tui.streaming import start_agent_worker`. In `run_agent()`, replace the comment on line 105 (`# Actual agent execution is handled by streaming worker (Plan 03-03)`) with `start_agent_worker(self, agent_name, prompt)`. This makes Ctrl+P, Ctrl+E, Ctrl+R all invoke the streaming worker.

2. **src/tui/actions.py** — In `send_prompt()`, after `prepare_agent_run(app, "plan")` returns a prompt (line 67), add a call to `start_agent_worker(app, "plan", prompt)` before returning. Import `start_agent_worker` from `src.tui.streaming` at the top of the file (inside the `TYPE_CHECKING` block is NOT sufficient since it runs at runtime — use a regular import or a lazy import to avoid circular imports). Note: streaming.py already imports from actions.py, so to avoid circular imports, use a local import inside `send_prompt()`: `from src.tui.streaming import start_agent_worker`. Restructure `send_prompt()` to: (a) call `get_prompt_text(app)` to get prompt, (b) if None return None, (c) call `prepare_agent_run(app, "plan")`, (d) call `start_agent_worker(app, "plan", prompt)` via local import, (e) return prompt.

3. **src/tui/status_bar.py** — Change line 27 from `"Enter a prompt and press Ctrl+Enter"` to `"Enter a prompt and press Ctrl+S"`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/projects/ai-agent-console && python -c "from src.tui.app import AgentConsoleApp; from src.tui.streaming import start_agent_worker; print('imports OK')" && python -c "from src.tui.status_bar import StatusBar; s = StatusBar(); assert 'Ctrl+S' in s._next_action, f'Got: {s._next_action}'; print('status bar OK')" && python -c "import inspect; from src.tui.app import AgentConsoleApp; src = inspect.getsource(AgentConsoleApp.run_agent); assert 'start_agent_worker' in src, 'run_agent missing start_agent_worker call'; print('run_agent wiring OK')" && python -c "import inspect; from src.tui.actions import send_prompt; src = inspect.getsource(send_prompt); assert 'start_agent_worker' in src, 'send_prompt missing start_agent_worker call'; print('send_prompt wiring OK')"</automated>
  </verify>
  <done>
    - app.py run_agent() calls start_agent_worker(self, agent_name, prompt) after prepare_agent_run
    - actions.py send_prompt() calls start_agent_worker(app, "plan", prompt) after prepare_agent_run
    - status_bar.py shows "Ctrl+S" in default hint text
    - No circular import errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests verifying streaming worker invocation from keyboard actions</name>
  <files>tests/test_tui_keys.py</files>
  <action>
Add 3 new tests to tests/test_tui_keys.py that verify the wiring gap is closed. Use unittest.mock.patch to mock start_agent_worker (patching at `src.tui.app.start_agent_worker` for run_agent tests, and `src.tui.actions.start_agent_worker` for send_prompt test) so no actual Claude CLI call is made:

1. `test_run_agent_calls_start_agent_worker` — Create app via run_test(), set prompt text to "Build something", call `app.run_agent("plan")`, assert that the mock `start_agent_worker` was called once with args (app, "plan", "Build something").

2. `test_send_prompt_calls_start_agent_worker` — Create app via run_test(), set prompt text to "Build an API", call `send_prompt(app)`, assert mock was called once with args (app, "plan", "Build an API").

3. `test_status_bar_default_hint_says_ctrl_s` — Create app via run_test(), get status bar, assert `"Ctrl+S"` in `app.status_bar.display_text` and `"Ctrl+Enter"` NOT in `app.status_bar.display_text`.

Add `from unittest.mock import patch, MagicMock` to the imports if not already present.
  </action>
  <verify>
    <automated>cd /home/ubuntu/projects/ai-agent-console && python -m pytest tests/test_tui_keys.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All existing 14 tests in test_tui_keys.py still pass
    - 3 new tests pass confirming: run_agent calls start_agent_worker, send_prompt calls start_agent_worker, status bar shows Ctrl+S
  </done>
</task>

</tasks>

<verification>
1. All tests pass: `python -m pytest tests/test_tui_keys.py tests/test_tui_streaming.py tests/test_tui_layout.py -x -v`
2. No circular import: `python -c "from src.tui.app import AgentConsoleApp"`
3. Grep confirms wiring: `grep -n "start_agent_worker" src/tui/app.py src/tui/actions.py`
4. Status bar fix: `python -c "from src.tui.status_bar import StatusBar; s=StatusBar(); assert 'Ctrl+S' in s._next_action"`
</verification>

<success_criteria>
- start_agent_worker is called from both run_agent() in app.py and send_prompt() in actions.py
- All 17+ tests pass (14 existing + 3 new)
- No circular import errors
- Status bar default text says "Ctrl+S" not "Ctrl+Enter"
- The broken key link (app keyboard action -> streaming.start_agent_worker) is now connected
</success_criteria>

<output>
After completion, create `.planning/phases/03-tui-shell/03-04-SUMMARY.md`
</output>
