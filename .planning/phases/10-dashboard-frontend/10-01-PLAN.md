---
phase: 10-dashboard-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/routers/views.py
  - src/templates/base.html
  - src/templates/task_list.html
  - src/server/app.py
  - tests/test_views.py
autonomous: true
requirements:
  - DASH-01
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "GET / returns HTML page with task list rendered by Alpine.js"
    - "Task list page includes a create-task form with prompt textarea and mode selector"
    - "HTML pages include Pico CSS and Alpine.js CDN links"
    - "All page routes require HTTP Basic Auth (401 without credentials)"
  artifacts:
    - path: "src/server/routers/views.py"
      provides: "HTML page routes (/, /tasks/{id}/view)"
      exports: ["view_router"]
    - path: "src/templates/base.html"
      provides: "Jinja2 base layout with Pico CSS + Alpine.js CDN"
      contains: "picocss"
    - path: "src/templates/task_list.html"
      provides: "Task list page with create form"
      contains: "x-data"
    - path: "tests/test_views.py"
      provides: "Integration tests for HTML page routes"
      contains: "test_task_list_page"
  key_links:
    - from: "src/server/app.py"
      to: "src/server/routers/views.py"
      via: "app.include_router(view_router)"
      pattern: "view_router"
    - from: "src/templates/task_list.html"
      to: "/tasks"
      via: "Alpine.js fetch() call to JSON API"
      pattern: "fetch.*tasks"
---

<objective>
Create the dashboard task list page with task creation form, served as Jinja2 templates with Alpine.js interactivity and Pico CSS styling.

Purpose: Delivers the primary dashboard view where users see all tasks and create new ones -- the main entry point for the web UI.
Output: Views router, base template, task list template, integration tests.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dashboard-frontend/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/server/dependencies.py:
```python
def verify_credentials(credentials: HTTPBasicCredentials = Depends(security)) -> str:
    """Verify HTTP Basic Auth credentials. Returns username on success."""
async def get_task_manager(request: Request) -> TaskManager:
    """Extract the TaskManager from app state."""
```

From src/server/routers/tasks.py:
```python
class TaskResponse(BaseModel):
    id: Optional[int] = None
    name: str
    project_path: str
    status: str
    mode: str
    prompt: str
    created_at: datetime
    completed_at: Optional[datetime] = None
    error: Optional[str] = None

class TaskListResponse(BaseModel):
    tasks: list[TaskResponse]
    count: int

class TaskCreate(BaseModel):
    prompt: str
    mode: str = "autonomous"
```

From src/server/app.py:
```python
def create_app() -> FastAPI:
    app = FastAPI(title="AI Agent Console", lifespan=lifespan)
    app.include_router(health_router)
    app.include_router(task_router)
    app.include_router(ws_router)
    return app
```

From src/server/config.py:
```python
class Settings(BaseSettings):
    auth_username: str = "admin"
    auth_password: str = "changeme"
    model_config = {"env_prefix": "APP_"}
```

Test auth pattern (from tests/test_task_endpoints.py):
```python
AUTH_HEADERS = {
    "Authorization": "Basic " + base64.b64encode(b"admin:changeme").decode()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create views router, base template, and task list page</name>
  <files>src/server/routers/views.py, src/templates/base.html, src/templates/task_list.html, src/server/app.py</files>
  <action>
1. Create `src/templates/` directory.

2. Create `src/server/routers/views.py` with a `view_router` APIRouter that has `Depends(verify_credentials)` as a router-level dependency (same pattern as task_router). Add two routes:
   - `GET /` -> returns `templates.TemplateResponse(request, "task_list.html")` with `response_class=HTMLResponse`
   - `GET /tasks/{task_id}/view` -> returns `templates.TemplateResponse(request, "task_detail.html", context={"task_id": task_id})` with `response_class=HTMLResponse`
   Use `Jinja2Templates(directory=str(Path(__file__).resolve().parent.parent.parent / "templates"))` to resolve the template directory relative to the source tree (avoids CWD-dependent path issues per research pitfall #2). Import `HTMLResponse` from `fastapi.responses`.

3. Create `src/templates/base.html` with:
   - DOCTYPE html, `<html lang="en" data-theme="light">`
   - Meta charset utf-8, viewport meta tag
   - `<title>` block: `{% block title %}AI Agent Console{% endblock %}`
   - Pico CSS v2 CDN: `<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">`
   - Alpine.js v3 CDN: `<script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3/dist/cdn.min.js"></script>`
   - Minimal custom `<style>` block for status color indicators using CSS custom properties:
     `[data-status="running"] { color: #0ea5e9; }` (cyan), `[data-status="completed"] { color: #22c55e; }` (green), `[data-status="failed"] { color: #ef4444; }` (red), `[data-status="queued"] { color: #6b7280; }` (grey), `[data-status="awaiting_approval"] { color: #f59e0b; }` (amber), `[data-status="cancelled"] { color: #9ca3af; }` (light grey). Also `.log-output { max-height: 70vh; overflow-y: auto; font-size: 0.85em; }`
   - `<nav class="container">` with app title and Tasks link
   - `<main class="container">{% block content %}{% endblock %}</main>`

4. Create `src/templates/task_list.html` extending base.html:
   - `{% extends "base.html" %}` and `{% block content %}`
   - A task creation form using Alpine.js `x-data` with fields: `prompt` (textarea, required), `mode` (select with "autonomous" and "supervised" options), `submitting` (boolean). On `@submit.prevent`, POST to `/tasks` with JSON body `{prompt, mode}`, then redirect to `/tasks/{id}/view` on success. Show `aria-busy` on button while submitting.
   - A task list section with Alpine.js `x-data="taskList()"` and `x-init="loadTasks()"`. The `taskList()` function fetches `GET /tasks` (with `credentials: 'same-origin'`), stores `data.tasks` in reactive state. Render with `<template x-for="task in tasks" :key="task.id">` inside `<article>` elements (Pico CSS semantic HTML, NOT div.card). Each article shows: task name (`x-text="task.name"`), status with `data-status` attribute (`x-bind:data-status="task.status"`, `x-text="task.status"`), mode, created_at, and a link to `/tasks/{task.id}/view`.
   - Use `{% raw %}` blocks around any Alpine.js `{{ }}` expressions to avoid Jinja2 delimiter collision. Prefer `x-text` and `x-bind` directives over `{{ }}` where possible.
   - Add auto-refresh: `setInterval(() => loadTasks(), 5000)` inside `x-init` so the task list updates without manual refresh.

5. Update `src/server/app.py`:
   - Import `view_router` from `src.server.routers.views`
   - Add `app.include_router(view_router)` in `create_app()` AFTER the other routers

Do NOT create task_detail.html yet -- that is Plan 02.
  </action>
  <verify>
    <automated>python -m pytest tests/test_views.py -x -q 2>/dev/null || echo "tests not yet created"</automated>
  </verify>
  <done>GET / returns HTML page with task list Alpine.js component and create form. GET /tasks/{id}/view route exists. Base template includes Pico CSS and Alpine.js CDN links. All view routes require auth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for view routes</name>
  <files>tests/test_views.py</files>
  <behavior>
    - test_task_list_page: GET / with auth returns 200, response contains "task_list" or "taskList" Alpine component, contains create form elements (textarea, select)
    - test_task_list_requires_auth: GET / without auth returns 401
    - test_task_detail_page: GET /tasks/1/view with auth returns 200 (even if task doesn't exist in DB -- template just needs task_id)
    - test_base_template_includes: GET / response HTML contains Pico CSS CDN link and Alpine.js CDN link
    - test_create_form_present: GET / response contains form with method POST action, textarea for prompt, select for mode
  </behavior>
  <action>
Create `tests/test_views.py` following the exact pattern from `tests/test_task_endpoints.py`:
- Same `TEST_DSN`, `AUTH_HEADERS`, `BAD_AUTH_HEADERS` constants
- Same `app_with_pool` fixture (lifespan context manager)
- Same `client` fixture (HTTPX ASGITransport + AsyncClient)
- `pytestmark = pytest.mark.asyncio`

Write 5 tests:

1. `test_task_list_page`: GET `/` with AUTH_HEADERS. Assert status 200, assert `text/html` in content-type, assert response text contains `x-data` (Alpine.js component) and `loadTasks` (fetch function).

2. `test_task_list_requires_auth`: GET `/` without auth headers. Assert status 401.

3. `test_task_detail_page`: GET `/tasks/999/view` with AUTH_HEADERS. Assert status 200, assert `text/html` in content-type. (Template renders even if task_id 999 doesn't exist in DB -- the detail page loads task data via client-side fetch.)

4. `test_base_template_includes`: GET `/` with AUTH_HEADERS. Assert response text contains `picocss/pico@2` (CDN link) and `alpinejs@3` (CDN link).

5. `test_create_form_present`: GET `/` with AUTH_HEADERS. Assert response text contains `<textarea` and `<select` and `supervised` and `autonomous`.

Run tests RED first (before Task 1 creates the files, tests should fail). Then after Task 1, run GREEN.

Note: Since both tasks are in the same plan and executed sequentially, write tests first, confirm they fail, then implement Task 1. However, since Task 1 is listed first for logical ordering, the executor should write tests (this task) alongside Task 1 implementation -- write tests, see them fail, then create implementation, then confirm green.
  </action>
  <verify>
    <automated>python -m pytest tests/test_views.py -x -q</automated>
  </verify>
  <done>All 5 tests pass. GET / returns HTML with task list, create form, Pico CSS, Alpine.js. Auth is enforced. GET /tasks/{id}/view returns HTML.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_views.py -x -q` -- all 5 view tests pass
- `python -m pytest tests/ -x -q` -- full test suite still passes (no regressions)
- GET / returns HTML with task list and create form
- Base template includes Pico CSS and Alpine.js CDN links
</verification>

<success_criteria>
1. Browser navigating to / (with auth) sees an HTML page with task list and create form
2. Task list fetches from GET /tasks API and renders task cards with status indicators
3. Create form POSTs to /tasks API and redirects to task detail view
4. All view routes return 401 without credentials
5. 5 integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-frontend/10-01-SUMMARY.md`
</output>
