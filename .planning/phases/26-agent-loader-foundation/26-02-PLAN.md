---
phase: 26-agent-loader-foundation
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/agents/config.py
  - tests/test_agent_config.py
autonomous: true
requirements:
  - AGLD-03
  - AGLD-04

must_haves:
  truths:
    - "get_project_registry() returns a new dict each call -- never mutates DEFAULT_REGISTRY"
    - "Project agents appear in the merged registry alongside core agents"
    - "Core agents (plan/execute/test/review) cannot be overridden by project agents with matching names"
    - "A warning is logged when a project agent conflicts with a core agent name"
    - "Two concurrent calls to get_project_registry() with different project paths produce independent registries"
    - "Functions that read the registry accept an optional registry parameter for dynamic agents"
  artifacts:
    - path: "src/agents/config.py"
      provides: "DEFAULT_REGISTRY, PROTECTED_AGENTS, get_project_registry, merge_registries, registry-aware functions"
      exports: ["DEFAULT_REGISTRY", "AGENT_REGISTRY", "PROTECTED_AGENTS", "get_project_registry", "merge_registries"]
      contains: "PROTECTED_AGENTS"
    - path: "tests/test_agent_config.py"
      provides: "Tests for registry isolation, core protection, merge behavior"
      min_lines: 100
  key_links:
    - from: "src/agents/config.py"
      to: "src/agents/loader.py"
      via: "get_project_registry calls discover_project_agents"
      pattern: "discover_project_agents"
    - from: "src/agents/config.py"
      to: "PROTECTED_AGENTS"
      via: "merge_registries checks against protected set"
      pattern: "PROTECTED_AGENTS"
---

<objective>
Add registry merge logic with core agent protection and per-project isolation.

Purpose: Concurrent tasks on different projects must each see their own agents without cross-contamination or mutation of the global default registry. Core pipeline agents must be immune to override.
Output: `merge_registries()`, `get_project_registry()`, registry-aware function signatures, comprehensive tests.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-agent-loader-foundation/26-RESEARCH.md
@.planning/phases/26-agent-loader-foundation/26-01-SUMMARY.md

@src/agents/config.py
@src/agents/loader.py
@tests/test_agent_config.py
</context>

<interfaces>
<!-- From Plan 01 output - executor needs these -->

From src/agents/loader.py (created in Plan 01):
```python
def discover_project_agents(project_path: str) -> dict[str, AgentConfig]:
    """Scan .claude/agents/*.md and return dict of agent_name -> AgentConfig."""
```

From src/agents/config.py (extended in Plan 01):
```python
@dataclass(frozen=True)
class AgentConfig:
    name: str
    system_prompt_file: str
    description: str = ""
    output_sections: list[str] = field(default_factory=list)
    next_agent: str | None = None
    allowed_transitions: tuple[str, ...] = ()
    system_prompt_inline: str | None = None
    source: str = "default"
    file_path: str | None = None
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add merge_registries and get_project_registry with core protection</name>
  <files>src/agents/config.py, tests/test_agent_config.py</files>
  <behavior>
    - test_project_registry_is_isolated: get_project_registry(path_with_agents) returns a new dict; modifying it does not affect DEFAULT_REGISTRY
    - test_default_registry_unchanged: after calling get_project_registry() and merge_registries(), DEFAULT_REGISTRY still has exactly 4 agents with source="default"
    - test_core_agents_protected: merge_registries with a project agent named "plan" skips it; merged result contains original core "plan"
    - test_core_override_logs_warning: merging a project agent named "execute" logs a warning containing "conflicts with core agent"
    - test_merge_adds_project_agents: merge_registries with project agents "db-migrator" and "api-tester" adds both to result alongside core agents
    - test_get_project_registry_no_path: get_project_registry(None) returns copy of DEFAULT_REGISTRY
    - test_get_project_registry_no_agents_dir: get_project_registry("/nonexistent") returns copy of DEFAULT_REGISTRY
  </behavior>
  <action>
Modify `src/agents/config.py`:

1. Rename `AGENT_REGISTRY` to `DEFAULT_REGISTRY`. Add backward-compat alias: `AGENT_REGISTRY = DEFAULT_REGISTRY`.

2. Add `PROTECTED_AGENTS = frozenset({"plan", "execute", "test", "review"})`.

3. Add `merge_registries(default: dict[str, AgentConfig], project: dict[str, AgentConfig]) -> dict[str, AgentConfig]`:
   - Create shallow copy: `merged = dict(default)`
   - Iterate project agents; if name in PROTECTED_AGENTS, log warning and skip; otherwise add to merged
   - Return merged

4. Add `get_project_registry(project_path: str | None = None) -> dict[str, AgentConfig]`:
   - If project_path is None or empty, return `dict(DEFAULT_REGISTRY)`
   - Import `discover_project_agents` from `src.agents.loader` (lazy import to avoid circular)
   - Call discover, then merge_registries, return result

5. Update existing functions to accept optional `registry` parameter with backward-compatible defaults:
   - `build_agent_enum(registry=None)` -- uses `registry if registry is not None else AGENT_REGISTRY`
   - `build_agent_descriptions(registry=None)` -- same pattern
   - `validate_transition(from_agent, to_agent, registry=None)` -- same pattern
   - `get_agent_config(name, registry=None)` -- same pattern
   - `resolve_pipeline_order(start_agent="plan", registry=None)` -- same pattern

6. Move `import logging` to module level: `log = logging.getLogger(__name__)`. Remove the inline `import logging` inside `validate_transition`.

Add tests to `tests/test_agent_config.py` -- write tests FIRST, then implement. Use `tmp_path` fixture for project agent files where needed. Use `unittest.mock.patch` to mock `discover_project_agents` where filesystem setup is not needed.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_agent_config.py tests/test_agent_loader.py -x -v</automated>
  </verify>
  <done>DEFAULT_REGISTRY is canonical name, AGENT_REGISTRY is backward-compat alias. merge_registries protects core agents and logs warnings. get_project_registry builds isolated per-project registries. All functions accept optional registry parameter. All tests pass (new + existing).</done>
</task>

</tasks>

<verification>
python3 -m pytest tests/test_agent_config.py tests/test_agent_loader.py -x -v
python3 -m pytest tests/ -x
</verification>

<success_criteria>
- DEFAULT_REGISTRY exists with 4 core agents, AGENT_REGISTRY is backward-compat alias
- PROTECTED_AGENTS frozenset contains plan/execute/test/review
- merge_registries() creates new dict, skips protected names with warning
- get_project_registry() returns isolated registry per project path
- All config functions accept optional registry parameter (backward compatible)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/26-agent-loader-foundation/26-02-SUMMARY.md`
</output>
