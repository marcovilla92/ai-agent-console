---
phase: 17-spa-frontend
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/server/app.py
  - src/server/routers/views.py
  - src/templates/base.html
  - src/templates/task_list.html
  - src/templates/task_detail.html
  - Dockerfile
  - tests/test_views.py
  - tests/test_spa_frontend.py
autonomous: false
requirements: [SPA-01]

must_haves:
  truths:
    - "GET / returns the Alpine.js SPA from static/index.html (not Jinja2)"
    - "Old view routes (/tasks/{id}/view) return 404"
    - "src/templates/ directory and views.py are deleted"
    - "Docker build includes static/ directory and serves SPA correctly"
    - "All existing tests pass (full suite green)"
  artifacts:
    - path: "src/server/app.py"
      provides: "FileResponse for / serving static/index.html, view_router removed"
      contains: "FileResponse"
    - path: "Dockerfile"
      provides: "COPY static/ for Docker deployment"
      contains: "COPY static/"
  key_links:
    - from: "src/server/app.py"
      to: "static/index.html"
      via: "FileResponse serving"
      pattern: "FileResponse.*index.html"
---

<objective>
Wire the SPA into the server, remove Jinja2 templates and views.py, update Dockerfile, and clean up tests.

Purpose: Complete the SPA-01 requirement by removing the old Jinja2 rendering and making static/index.html the sole entry point.
Output: Updated app.py, Dockerfile; deleted views.py and src/templates/; updated test files
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-spa-frontend/17-RESEARCH.md
@.planning/phases/17-spa-frontend/17-01-SUMMARY.md
@src/server/app.py
@src/server/routers/views.py
@Dockerfile
@tests/test_views.py
@tests/test_spa_frontend.py

<interfaces>
<!-- From app.py -- current wiring to change -->
from src.server.routers.views import view_router  # REMOVE this import
app.include_router(view_router)                    # REMOVE this line

<!-- Add instead -->
from pathlib import Path
from fastapi.responses import FileResponse
STATIC_DIR = Path(__file__).resolve().parent.parent.parent / "static"

@app.get("/", response_class=FileResponse)
async def root():
    return FileResponse(STATIC_DIR / "index.html")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server wiring + cleanup + Dockerfile</name>
  <files>src/server/app.py, Dockerfile, src/server/routers/views.py, src/templates/base.html, src/templates/task_list.html, src/templates/task_detail.html, tests/test_views.py, tests/test_spa_frontend.py</files>
  <action>
**Step 1: Update src/server/app.py**
- Remove the import: `from src.server.routers.views import view_router`
- Remove the line: `app.include_router(view_router)`
- Add imports: `from pathlib import Path` and `from fastapi.responses import FileResponse`
- Add at module level: `STATIC_DIR = Path(__file__).resolve().parent.parent.parent / "static"`
- Add a GET / route in the create_app function BEFORE the router includes (or as a standalone route added to the app). The route must have the auth dependency since the old view_router had it:

```python
from src.server.dependencies import verify_credentials

@app.get("/")
async def root(_=Depends(verify_credentials)):
    return FileResponse(STATIC_DIR / "index.html")
```

Note: The root route must be added directly on the app (not via a router) to avoid prefix conflicts. Add it after `app = FastAPI(...)` but before the other router includes.

**Step 2: Delete old files**
- Delete `src/server/routers/views.py`
- Delete `src/templates/base.html`
- Delete `src/templates/task_list.html`
- Delete `src/templates/task_detail.html`
- Delete `src/templates/` directory (should be empty after above)

**Step 3: Update Dockerfile**
- Add `COPY static/ ./static/` line after the existing `COPY templates/ ./templates/` line
- Also remove `COPY src/ ./src/` line's assumption that src/templates exists -- but actually src/templates is under src/ so deleting those files is fine, the COPY src/ will just copy less

**Step 4: Update tests/test_views.py**
- Delete the entire file. It tests the old Jinja2 view routes (task_list_page, task_detail_page) which no longer exist. The test for /tasks/{id}/outputs (test_get_task_outputs_empty, test_get_task_outputs_requires_auth) should stay -- but those are testing task_router endpoints, not view routes. Check: the outputs endpoint is in tasks.py router, not views.py, so those tests likely exist in test_task_endpoints.py already. If so, delete test_views.py entirely. If not, move the outputs tests to test_task_endpoints.py before deleting.

Actually, looking at test_views.py: tests test_get_task_outputs_empty and test_get_task_outputs_requires_auth test GET /tasks/{id}/outputs which is on the task_router. These can stay or be moved. Simplest: delete the whole file since the outputs endpoint is covered by test_task_endpoints.py.

**Step 5: Add server integration tests to tests/test_spa_frontend.py**
Add these server-response tests (using app_with_pool fixture + httpx AsyncClient, same pattern as test_views.py):
- `test_root_returns_spa_html` -- GET / with auth returns 200, text/html, body contains "Alpine.store"
- `test_old_view_routes_removed` -- GET /tasks/1/view with auth returns 404
- `test_root_requires_auth` -- GET / without auth returns 401

Import and fixture pattern:
```python
from src.server.app import create_app
# Use app_with_pool fixture from conftest or inline as in test_views.py
```
  </action>
  <verify>
    <automated>cd /home/ubuntu/projects/ai-agent-console && python -m pytest tests/test_spa_frontend.py tests/test_task_endpoints.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>GET / returns static/index.html SPA. Old /tasks/{id}/view route returns 404. views.py and src/templates/ deleted. Dockerfile copies static/. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify SPA end-to-end in browser</name>
  <files>none</files>
  <action>
Human verifies the complete SPA wizard flow in a browser. All automation is done -- this checkpoint confirms visual and interactive correctness.

What was built: Complete SPA frontend replacing Jinja2 templates. Four-view wizard: project selection with stack badges, project creation with template picker, prompt composition with phase suggestion and context preview, running task with WebSocket streaming and approval gates.
  </action>
  <verify>
    <automated>cd /home/ubuntu/projects/ai-agent-console && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms all 4 views work correctly in the browser. Full test suite passes.</done>
  <how-to-verify>
    1. Open the console URL in browser (should prompt for HTTP Basic Auth)
    2. Verify project selection view loads with project cards showing name, stack, and relative time
    3. Click "New Project" -- verify creation form with template dropdown appears
    4. Click back, select an existing project -- verify prompt view with phase suggestion
    5. Click "Show context" -- verify context preview expands
    6. Type a test prompt, select mode, click "Run Task" -- verify streaming output appears
    7. If supervised mode: verify approval UI appears when needed
    8. After task completes: verify "Back to Projects" button works
    9. Run `python -m pytest tests/ -x -q` -- full test suite should be green
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` -- full test suite green (no test_views.py failures)
- `python -m pytest tests/test_spa_frontend.py -x -q` -- all tests pass including test_old_view_routes_removed
- `grep "view_router" src/server/app.py` -- returns nothing
- `ls src/templates/` -- directory does not exist
- `ls src/server/routers/views.py` -- file does not exist
- `grep "COPY static/" Dockerfile` -- line exists
- `grep "FileResponse" src/server/app.py` -- line exists
</verification>

<success_criteria>
- Single index.html serves as the entire frontend (SPA-01 complete)
- All Jinja2 templates and views.py removed
- Dockerfile updated for Docker deployment
- Full test suite passes
- Human verified the 4-view wizard flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/17-spa-frontend/17-02-SUMMARY.md`
</output>
