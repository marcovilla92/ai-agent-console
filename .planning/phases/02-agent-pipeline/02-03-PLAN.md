# Plan 02-03: Pipeline Runner, Handoffs & Project Creation

## Goal
Implement the sequential pipeline (PLAN→EXECUTE→REVIEW) with visible handoffs (AGNT-04) and project creation flow (INFR-04).

## Tasks

1. Create `src/pipeline/runner.py`:
   - `async run_pipeline(prompt: str, project_path: str, db: aiosqlite.Connection) -> PipelineResult`
   - Sequential flow: PLAN -> EXECUTE -> REVIEW
   - Each step builds handoff context from previous agent's output
   - PipelineResult dataclass: session_id, steps (list[AgentResult]), final_decision (str)

2. Create `src/pipeline/handoff.py`:
   - `build_handoff(from_result: AgentResult) -> str`: Formats handoff data as visible, structured text
   - Handoff includes: source agent name, key sections, timestamp

3. Create `src/pipeline/project.py`:
   - `create_project(name: str, workspace_root: str) -> str`: Creates project folder, returns path
   - Sanitizes name (alphanumeric + hyphens)
   - Creates standard structure (src/, README.md placeholder)

4. Tests:
   - `tests/test_pipeline.py`: Pipeline runs 3 agents in order, produces PipelineResult
   - `tests/test_handoff.py`: Handoff formatting includes source agent and sections
   - `tests/test_project.py`: Project creation creates folder, handles duplicates

## Files to Create
- src/pipeline/__init__.py
- src/pipeline/runner.py
- src/pipeline/handoff.py
- src/pipeline/project.py
- tests/test_pipeline.py
- tests/test_handoff.py
- tests/test_project.py

## Success Criteria
- Pipeline runs PLAN→EXECUTE→REVIEW in sequence
- Handoff data between agents is structured and inspectable
- Project creation creates a dedicated workspace folder
- All tests pass
