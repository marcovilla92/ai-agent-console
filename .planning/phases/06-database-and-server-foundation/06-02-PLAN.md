---
phase: 06-database-and-server-foundation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/server/__init__.py
  - src/server/app.py
  - src/server/config.py
  - src/server/dependencies.py
  - src/pipeline/protocol.py
  - src/pipeline/orchestrator.py
  - tests/test_server.py
  - tests/test_protocol.py
autonomous: true
requirements: [INFR-01]

must_haves:
  truths:
    - "FastAPI server starts and responds to a health-check endpoint with 200"
    - "Health endpoint verifies database connectivity (SELECT 1)"
    - "Connection pool is created on startup and closed on shutdown via lifespan"
    - "Orchestrator accepts TaskContext Protocol instead of AgentConsoleApp"
    - "A non-TUI class can satisfy TaskContext Protocol (structural subtyping)"
  artifacts:
    - path: "src/server/app.py"
      provides: "FastAPI app factory with lifespan-managed asyncpg pool"
      exports: ["create_app"]
    - path: "src/server/config.py"
      provides: "pydantic-settings configuration"
      exports: ["Settings", "get_settings"]
    - path: "src/server/dependencies.py"
      provides: "FastAPI dependency for pool injection"
      exports: ["get_pool"]
    - path: "src/pipeline/protocol.py"
      provides: "TaskContext Protocol definition"
      exports: ["TaskContext"]
    - path: "src/pipeline/orchestrator.py"
      provides: "Orchestrator refactored to use TaskContext and asyncpg.Pool"
      contains: "ctx: TaskContext"
    - path: "tests/test_server.py"
      provides: "Server integration tests"
      min_lines: 40
    - path: "tests/test_protocol.py"
      provides: "Protocol conformance tests"
      min_lines: 30
  key_links:
    - from: "src/server/app.py"
      to: "src/db/migrations.py"
      via: "calls apply_schema in lifespan"
      pattern: "apply_schema"
    - from: "src/server/app.py"
      to: "asyncpg"
      via: "creates pool in lifespan, stores on app.state"
      pattern: "asyncpg\\.create_pool"
    - from: "src/pipeline/orchestrator.py"
      to: "src/pipeline/protocol.py"
      via: "imports and uses TaskContext Protocol"
      pattern: "from src\\.pipeline\\.protocol import TaskContext"
    - from: "src/server/dependencies.py"
      to: "src/server/app.py"
      via: "extracts pool from request.app.state"
      pattern: "request\\.app\\.state\\.pool"
---

<objective>
Create FastAPI server shell with health endpoint, lifespan-managed pool, and decouple the orchestrator from TUI via TaskContext Protocol.

Purpose: The server is the entry point for the entire v2.0 web platform. The Protocol decoupling enables the orchestrator to serve both TUI (v1.0) and web (v2.0) frontends.
Output: Server app factory, config, health endpoint, TaskContext Protocol, refactored orchestrator.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-database-and-server-foundation/06-RESEARCH.md
@.planning/phases/06-database-and-server-foundation/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 06-01 outputs (pg_repository and migrations) -->

From src/db/pg_repository.py (created in 06-01):
```python
class TaskRepository:
    def __init__(self, pool: asyncpg.Pool) -> None: ...
    async def create(self, task: Task) -> int: ...
    async def get(self, task_id: int) -> Task | None: ...
    async def list_all(self) -> list[Task]: ...
```

From src/db/migrations.py (created in 06-01):
```python
async def apply_schema(pool: asyncpg.Pool) -> None: ...
```

<!-- Existing orchestrator signature to refactor -->

From src/pipeline/orchestrator.py:
```python
async def orchestrate_pipeline(
    app: AgentConsoleApp,
    prompt: str,
    db: aiosqlite.Connection | None = None,
    session_id: int | None = None,
) -> OrchestratorState: ...

async def log_decision(
    db: aiosqlite.Connection,
    session_id: int,
    decision: OrchestratorDecision,
    iteration_count: int,
) -> int: ...

async def show_reroute_confirmation(app: AgentConsoleApp, decision: OrchestratorDecision) -> bool: ...
async def show_halt_dialog(app: AgentConsoleApp, state: OrchestratorState) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FastAPI server with lifespan pool and health endpoint</name>
  <files>src/server/__init__.py, src/server/app.py, src/server/config.py, src/server/dependencies.py, tests/test_server.py</files>
  <behavior>
    - test_health_endpoint: GET /health returns 200 with {"status": "ok", "database": "connected"}
    - test_health_db_check: health endpoint actually queries the database (SELECT 1)
    - test_lifespan_pool: pool exists on app.state during request, is created on startup and closed on shutdown
  </behavior>
  <action>
1. Create `src/server/__init__.py` (empty).

2. Create `src/server/config.py`:
   - `Settings(BaseSettings)` with fields: `database_url: str` (default: `postgresql://postgres:postgres@localhost:5432/agent_console`), `pool_min_size: int = 2`, `pool_max_size: int = 5`, `host: str = "0.0.0.0"`, `port: int = 8000`
   - `model_config = {"env_prefix": "APP_"}` so env vars are `APP_DATABASE_URL`, etc.
   - `@lru_cache def get_settings() -> Settings`

3. Create `src/server/dependencies.py`:
   - `async def get_pool(request: Request) -> asyncpg.Pool` that returns `request.app.state.pool`

4. Create `src/server/app.py`:
   - `@asynccontextmanager async def lifespan(app: FastAPI)` that:
     - Calls `get_settings()` for config
     - Creates pool via `asyncpg.create_pool(dsn=settings.database_url, min_size=settings.pool_min_size, max_size=settings.pool_max_size, command_timeout=60.0)`
     - Stores pool on `app.state.pool`
     - Calls `apply_schema(app.state.pool)` from `src.db.migrations`
     - Yields
     - In finally block: `await app.state.pool.close()`
   - `def create_app() -> FastAPI` that creates FastAPI instance with title="AI Agent Console", lifespan=lifespan
   - Health router with `GET /health` that acquires connection from pool, runs `SELECT 1`, returns `{"status": "ok", "database": "connected"}`
   - Include health router in create_app

5. Create `tests/test_server.py`:
   - Use httpx.AsyncClient with `ASGITransport(app=create_app())` for testing (FastAPI standard pattern)
   - Override settings via dependency override or env var to point to test database (`TEST_DATABASE_URL` env var, same as 06-01 tests)
   - Test all 3 behaviors above
   - For lifespan test: use `async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test")` context manager which triggers lifespan

Note: Use `from contextlib import asynccontextmanager` for lifespan. Do NOT use deprecated `@app.on_event("startup")`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/projects/ai-agent-console && python -m pytest tests/test_server.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>FastAPI app boots, creates asyncpg pool on startup, closes on shutdown. GET /health returns 200 with database connectivity confirmation. All 3 server tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TaskContext Protocol and refactor orchestrator</name>
  <files>src/pipeline/protocol.py, src/pipeline/orchestrator.py, tests/test_protocol.py</files>
  <behavior>
    - test_taskcontext_protocol: A simple class implementing all TaskContext methods satisfies isinstance(obj, TaskContext) check (runtime_checkable)
    - test_orchestrator_uses_protocol: orchestrate_pipeline signature accepts TaskContext (not AgentConsoleApp) and asyncpg.Pool (not aiosqlite.Connection)
    - test_log_decision_uses_pool: log_decision accepts asyncpg.Pool (not aiosqlite.Connection)
  </behavior>
  <action>
1. Create `src/pipeline/protocol.py`:
   - `@runtime_checkable class TaskContext(Protocol)` with these methods (derived from what orchestrator.py actually calls on `app`):
     - `async def update_status(self, agent: str, state: str, step: str, next_action: str) -> None` -- maps to `app.status_bar.set_status()`
     - `async def stream_output(self, agent_name: str, prompt: str, sections: dict) -> dict[str, str]` -- maps to `stream_agent_to_panel()` call
     - `async def confirm_reroute(self, next_agent: str, reasoning: str) -> bool` -- maps to `show_reroute_confirmation()`
     - `async def handle_halt(self, iteration_count: int) -> str` -- maps to `show_halt_dialog()`, returns "continue"|"approve"|"stop"
     - `@property def project_path(self) -> str` -- maps to `app.project_path`

2. Refactor `src/pipeline/orchestrator.py`:
   - Change `orchestrate_pipeline` signature: `app: AgentConsoleApp` -> `ctx: TaskContext`, `db: aiosqlite.Connection | None` -> `pool: asyncpg.Pool | None`
   - Remove `from src.tui.actions import AGENT_PANEL_MAP` and `from src.tui.streaming import stream_agent_to_panel` imports from inside the function
   - Replace `app.status_bar.set_status(...)` calls with `await ctx.update_status(...)`
   - Replace `stream_agent_to_panel(app, ...)` call with `sections = await ctx.stream_output(state.current_agent, agent_prompt, {})`
   - Replace `show_reroute_confirmation(app, decision)` with `await ctx.confirm_reroute(decision.next_agent, decision.reasoning)`
   - Replace `show_halt_dialog(app, state)` with `await ctx.handle_halt(state.iteration_count)`
   - Change `log_decision` to accept `pool: asyncpg.Pool` and use `OrchestratorDecisionRepository(pool)` from `src.db.pg_repository`
   - Replace `app.project_path` with `ctx.project_path`
   - Remove `import aiosqlite` and the `TYPE_CHECKING` block for `AgentConsoleApp`
   - The auto-commit block at the end: use `TaskRepository(pool)` instead of `SessionRepository(db)`
   - Remove `show_reroute_confirmation` and `show_halt_dialog` functions (their logic moves to TaskContext implementations)

3. Create `tests/test_protocol.py`:
   - Create a `MockTaskContext` class that implements all 5 methods/properties with simple returns (update_status does nothing, stream_output returns empty dict, confirm_reroute returns True, handle_halt returns "continue", project_path returns "/tmp/test")
   - Test `isinstance(MockTaskContext(), TaskContext)` is True (runtime_checkable)
   - Test `orchestrate_pipeline` function signature accepts TaskContext type (inspect.signature check or just verify import works)
   - Test `log_decision` signature accepts asyncpg.Pool type

Important: Do NOT break existing v1.0 TUI. The TUI's `AgentConsoleApp` will need a `TaskContext` adapter in a future phase -- for now, the orchestrator just accepts the Protocol. The TUI code that calls `orchestrate_pipeline` will need updating, but that is out of scope for this phase (TUI is being replaced by web in v2.0).
  </action>
  <verify>
    <automated>cd /home/ubuntu/projects/ai-agent-console && python -m pytest tests/test_protocol.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>TaskContext Protocol defined with 5 methods. orchestrate_pipeline accepts TaskContext + asyncpg.Pool. MockTaskContext satisfies Protocol check. log_decision uses pg_repository. All protocol tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_server.py tests/test_protocol.py -x -q` -- all tests pass
- `python -m pytest tests/test_pg_repository.py -x -q` -- plan 01 tests still pass
- `python -c "from src.server.app import create_app; print('server imports OK')"` -- no import errors
- `python -c "from src.pipeline.protocol import TaskContext; from src.pipeline.orchestrator import orchestrate_pipeline; print('protocol imports OK')"` -- no import errors
- Health endpoint returns 200 with database connectivity
</verification>

<success_criteria>
- FastAPI server starts with create_app() and responds to GET /health with 200
- asyncpg pool is lifespan-managed (created on startup, closed on shutdown)
- TaskContext Protocol is defined and runtime_checkable
- Orchestrator uses TaskContext instead of AgentConsoleApp
- Orchestrator uses asyncpg.Pool instead of aiosqlite.Connection
- All server and protocol tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-database-and-server-foundation/06-02-SUMMARY.md`
</output>
