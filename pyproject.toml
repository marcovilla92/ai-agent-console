[project]
name = "ai-agent-workflow-console"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "aiosqlite>=0.20",
    "tenacity>=8.0",
    "textual>=0.50",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]
